node_modules/
# На сервере /opt/aurum-panel — одновременно репозиторий и домашний каталог
# пользователя aurum, поэтому npm и системные инструменты складывают сюда
# кэши и SSH-настройки. Это состояние конкретной машины, не исходники.
.npm/
.cache/
.ssh/
dist/
build/
coverage/
*.tsbuildinfo
.env
.env.local
.env.*.local
.DS_Store
apps/api/prisma/*.db
dump.rdb

# Сборка Maven-плагинов из other-plugins
target/
.gradle/

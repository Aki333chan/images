package ovh.aurumgg.companion.core.model;

import java.util.UUID;

/**
 * Игрок, сидящий в тюрьме прямо сейчас.
 *
 * ТОЛЬКО ТЕ, КТО В СЕТИ, и это не упрощение. Признак «сидит» EssentialsX
 * хранит в файле игрока, а не в отдельном списке: чтобы найти всех сидящих,
 * пришлось бы прочитать userdata КАЖДОГО, кто когда-либо заходил, — на живом
 * сервере это тысячи обращений к диску на каждый показ страницы. Панель
 * дополняет этот список своими записями о тех, кого посадила сама, и вместе
 * они покрывают всё, что вообще можно покрыть дёшево.
 *
 * @param uuid      игрок
 * @param name      ник на момент опроса
 * @param jail      имя тюрьмы, в которой он сидит
 * @param releaseAt когда EssentialsX выпустит его сам, в миллисекундах эпохи;
 *                  0 — срок не назначен, сидит до отмены
 */
public record JailedPlayer(UUID uuid, String name, String jail, long releaseAt) {}

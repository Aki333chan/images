-- Язык панели, выбранный сотрудником вручную.
--
-- Nullable намеренно: NULL значит «как в браузере», и это не то же самое,
-- что 'ru'. Человек, ничего не выбиравший, должен получить свой системный
-- язык, а не русский по умолчанию строки.
ALTER TABLE "users" ADD COLUMN "locale" TEXT;
